diff --git a/packages/clis/parser/game-files/map-files-parser.ts b/packages/clis/parser/game-files/map-files-parser.ts
index b65d4c8..9c4aa68 100644
--- a/packages/clis/parser/game-files/map-files-parser.ts
+++ b/packages/clis/parser/game-files/map-files-parser.ts
@@ -129,7 +129,7 @@ export function parseMapFiles(
       ...postProcess(
         defData,
         sectorData,
-        parseIconMatFiles(map, entries),
+        process.env.LANE_GUIDE_SKIP_ICONS === '1' ? new Map() : parseIconMatFiles(map, entries),
         l10n,
       ),
     };
diff --git a/packages/clis/parser/game-files/prefab-ppd-parser.ts b/packages/clis/parser/game-files/prefab-ppd-parser.ts
index 10fdc04..6b7e8f9 100644
--- a/packages/clis/parser/game-files/prefab-ppd-parser.ts
+++ b/packages/clis/parser/game-files/prefab-ppd-parser.ts
@@ -456,10 +456,11 @@ export function parsePrefabPpd(buffer: Buffer): PrefabDescription {
       };
     }),
     semaphores: rawPrefab.semaphores.map(rs => {
-      const { pos, rot, type, intervals, cycle, profile } = rs;
+      const { pos, rot, type, id, intervals, cycle, profile } = rs;
       return {
         ...getXYZR(pos, rot),
         type,
+        id,
         intervals,
         cycle,
         profile,
diff --git a/packages/clis/parser/game-files/sii-parser.ts b/packages/clis/parser/game-files/sii-parser.ts
index 7d5e6cf..d9ad138 100644
--- a/packages/clis/parser/game-files/sii-parser.ts
+++ b/packages/clis/parser/game-files/sii-parser.ts
@@ -58,6 +58,12 @@ const Comment = createToken({
   pattern: /(\/\/|#).*/,
   group: Lexer.SKIPPED,
 });
+const BlockComment = createToken({
+  name: 'BlockComment',
+  pattern: /\/\*[\s\S]*?\*\//,
+  group: Lexer.SKIPPED,
+  line_breaks: true,
+});
 
 const allTokens = [
   SiiNunit,
@@ -77,6 +83,7 @@ const allTokens = [
   HexLiteral,
   Property,
   WhiteSpace,
+  BlockComment,
   Comment,
 ];
 
diff --git a/packages/libs/map/types.ts b/packages/libs/map/types.ts
index 43e9a36..8162ce7 100644
--- a/packages/libs/map/types.ts
+++ b/packages/libs/map/types.ts
@@ -585,6 +585,7 @@ export interface PrefabDescription {
     part: string;
   }[];
   semaphores: {
+    id: number;
     x: number;
     y: number;
     z: number;
